name: Deploy to S3 via Container

inputs:
  bucket-name:
    required: true
  bucket-region:
    required: true
  app-folder:
    required: true
outputs:
  website-url:
    description: Website url

runs:
  using: docker
  image: Dockerfile
